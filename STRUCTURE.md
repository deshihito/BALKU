# BALKU 構成

## 現行アーキテクチャ

現行のオンライン対戦盤面はBabylonベースの旧試作ではなく、**React DOMの `Home.tsx`** を使用する。対戦ルールの単一の正本はサーバー側の `balkuGame.ts` であり、クライアントはtRPC経由で状態を読み取り、行動だけを送信する。

| モジュール | 担当 |
|---|---|
| `client/src/pages/Home.tsx` | ロビー、チュートリアル、施工盤面、提出、BALKU、調達市場、自動オークション、最終精算モーダルを表示する。 |
| `client/src/index.css` | 暗い製図台、クリーム色の現場図面、設計図シアン、危険表示を定義する。 |
| `server/balkuGame.ts` | カードプール、固定山札、提出と効果、BALKU、調達市場、共通オークション、手番遷移、最終精算を管理する。 |
| `server/balkuGame.test.ts` | ルールの単体テストを管理する。 |
| `server/routers.ts` | ルーム作成・参加・開始・再戦・同期状態取得・行動のtRPC契約を提供する。 |
| `server/db.ts` / `drizzle/schema.ts` | ルーム状態と参加者情報をMySQL/TiDBへ保存し、楽観ロックで更新する。 |
| `TESTING.md` | 自動テストとブラウザ／同期検証の記録を保持する。 |

旧Babylon試作ファイルと不要なランタイムは独立版から除去し、現行のオンラインルートはReact DOMとNode/Expressだけで構成する。

## 状態同期

ゲーム状態は `gameRooms.gameState` にJSONで保存し、`revision` を用いて同時更新を検出する。クライアントは約2秒ごとに `balku.getRoom` を取得する。相手の手札、山札の内容、他社の伏せ企画が正規か違法かという情報は、レスポンス作成時に隠す。

## 重要な状態遷移

| 状態・処理 | ルール |
|---|---|
| 開始 | 人数×36枚の固定山札を生成し、全参加者へ8枚を配る。先手の追加配札は行わない。 |
| ドロー | 各手番開始時に最大3枚引く。山札は再生成しない。 |
| 行動 | 企画提出または直接調達で行動済みとなり、自動で次の施工者へ移る。BALKU・入札は手番外でも可能。 |
| 一巡開始 | 前回の自動オークションを決済し、山札があれば新しい1枚を共通市場へ出品する。 |
| 終局 | 山札が空になった場合でも進行中のオークションを決済し、各施工者が同数の最終ラウンドを完了した後に最終精算を行う。 |

## アセット

背景、ロゴ、カードの質感はCSSで表現し、外部ストレージURLへ依存しない。Renderへのデプロイ時に追加のアセットサーバーは不要である。
